# SOLAR.md — 太陽光発電モニタ機能

Renogy Rover Li MPPTチャージコントローラーの状態を RS485(Modbus RTU) で読み取り、
Home Control ダッシュボードに表示する機能。将来的にDC負荷のON/OFF制御も行う。

最終更新: 2026-08-27 / 状態: **サーバー側基盤（旧Job1・Job2）完了。VPNハブ方式で設計確定（§5.5）。
BT-2経由のBLEでRover疎通に成功しRover側は正常と確定。RJ45ピン配置の誤り（6/7ではなく1/2が
A/Bの可能性）が判明。次はピン1/2で直結RS485を再テストする（詳細は§5.6）**

---

## 1. 構成

**二経路構成（詳細は§5.5）。主経路はVPN経由のリアルタイム監視・制御、
副経路はESP32→EC2への直接POSTで、主経路が落ちても監視だけは生き残るフォールバック。**

```
[ソーラーパネル]─[Rover Li 20A MPPT]─[バッテリー12V]
                      │RJ45(RS485)   │LOAD端子(常時ON設定)
                      │A/B/GND(基準線)の3本 └─(ヒューズ2A)─┐
                      ▼                              ▼
              [Waveshare ESP32-S3-Relay-6CH] ← ESPHome / 自宅LAN・NAT内
                      │                     ＼
   主経路（LAN内・高頻度）│                      ＼ 副経路（フォールバック・5分間隔）
   ESPHome web_server API │                       ＼ HTTPS POST (Bearer認証)
   （solar-bridgeが操作） ▼                          ▼
              [OrangePi] solar-bridge（新規・systemd）  │
                      │ Tailscale VPN（OrangePi起点）    │
                      │ ※EC2からもOrangePiのTailscale IPへ│
                      │   直接到達可（トンネル確立後）     │
                      ▼                                  ▼
              [EC2] server/features/solar/routes.js ←────┘
                      │ SSE
                      ▼
              [ブラウザ] /home/solar/ 専用画面（iPhone含む。2026-08-27〜、Bus/Cat同様の専用タブ化）
```

ESP32・OrangePiは自宅NAT内にあるため、**VPNトンネルも含めてすべて自宅側デバイスが起点**で確立する
（Tailscaleの接続はOrangePiが自発的に張る。ホームルーターのポート開放は不要）。
トンネル確立後はEC2からOrangePiへ双方向に到達できるようになるが、これは
「EC2から自宅へ能動的に穴を開けにいく」設計ではなく「自宅側が自発的に張った経路を使う」設計として
CLAUDE.mdの原則の例外として扱う（§5.5・CLAUDE.md更新参照）。
副経路（ESP32→EC2直接POST）は従来通り片方向PUSHのみで、この原則を一切変えない。

### ハードウェア

| | |
|---|---|
| チャージコントローラー | Renogy Rover Li 20A（型番シール実測: **`RNG-CTRL-RVR20`のみ、G2表記は無い**。§5.6参照） |
| コントローラー基板 | Waveshare ESP32-S3-Relay-6CH（DC7〜36V入力・絶縁RS485・リレー6ch・DINレールケース） |
| ファームウェア | ESPHome |
| 電源 | RoverのLOAD端子から給電（バッテリーへの直接配線はしない） |
| **Bluetoothモジュール（発注済み・未着）** | **Renogy BT-2**（RS485ポートに挿す純正品。DC Homeアプリ対応。§5.6の切り分けに使用） |

**購入状況: 本体到着済み。BT-2は発注済み・未着。§5「開発フェーズ」に沿って開発中だが、
フェーズ1がModbus無応答で行き詰まりBT-2到着待ち（§5.6）。**

---

## 2. 完了済み（Job1・Job2）

### 追加・変更したファイル

| ファイル | 変更 |
|---|---|
| `server/features/solar/routes.js` | **新規** — `POST /report` / `GET /status` / `GET /stream` |
| `server/server.js` | +5行（require・mount・起動ログ） |
| `server/public/home/index.html` | +196行（🔋蓄電池カードとSSE購読） |
| `server/.env.example` | +3行（`SOLAR_SECRET`） |

既存コードの削除・書き換えは無く、すべて追記のみ。

**2026-08-27追記**: その後ダッシュボード組み込みから専用画面（`/home/solar/`）へ移行したため、
上記の`home/index.html`への追記は削除し、`server/public/solar/index.html`に切り出した
（詳細は§5.8）。`server/features/solar/routes.js`のAPI自体は変更無し。

### APIエンドポイント

| エンドポイント | 認証 | 説明 |
|---|---|---|
| `POST /home/api/solar/report` | Bearer | ESP32からの状態レポート受信 |
| `GET /home/api/solar/status` | 不要 | 現在の状態取得 |
| `GET /home/api/solar/stream` | 不要 | SSEリアルタイムストリーム |

Apache設定の追加は不要（`ProxyPass /home/api/` が既に通っている）。

### 検証済みの挙動

サーバーを実際に起動して12項目を確認済み。

- 認証 — トークン無し/誤り→401、正しい→200。`status`/`stream` は認証不要
- 不正値の拒否 — `soc:999` や `"abc"` を送っても**前回の正常値を維持する**
- 部分更新 — 一部のキーだけ送っても他のキーが消えない
- `soc:0` が欠測扱いされない（0は有効値として区別している）
- 不正な `chargingState` 文字列は無視（SSEへの混入を防ぐ）
- SSE — 接続直後に `init`、以降 `report` がpush

### 動作確認方法

```bash
cd server && node server.js
# 別ターミナル
curl -X POST localhost:3000/home/api/solar/report -H 'Content-Type: application/json' \
  -d '{"soc":73,"batteryVoltage":13.4,"chargingPower":88,"chargingState":"mppt","generationToday":1250,"fault":0}'
open http://localhost:3000/home/control/
```

---

## 3. API契約（フェーズ1の実装対象）

**この章は副経路（ESP32→EC2直接POST、フォールバック・監視専用）の契約。**
主経路（VPN経由・OrangePi solar-bridge、リアルタイム監視＋リレー制御）は§5.5参照。

`POST /home/api/solar/report` に以下のJSONを送る。**送らなかったキーは前回値が維持される**
（ESP32側で更新タイミングがずれても欠測表示にならないようにするため）。
範囲外の値は無視され前回値が残るので、通信エラー時のゴミが表示を壊すことはない。

```jsonc
{
  "soc":             87,      // % 0-100
  "batteryVoltage":  13.2,    // V 0-100
  "chargingCurrent": 4.35,    // A 0-200
  "chargingPower":   57,      // W 0-10000
  "solarVoltage":    18.4,    // V 0-200
  "solarCurrent":    3.1,     // A 0-200
  "loadPower":       12,      // W 0-10000
  "batteryTemp":     24,      // ℃ -50-120
  "controllerTemp":  31,      // ℃ -50-120
  "generationToday": 420,     // Wh 0-1000000
  "chargingState":   "mppt",  // 下の列挙のいずれか。範囲外の文字列は無視される
  "fault":           0        // 障害ビット。0なら正常
}
```

`chargingState` の取りうる値（Roverの 0x0120 下位バイトに対応）:
`deactivated` / `activated` / `mppt` / `equalizing` / `boost` / `float` / `current_limiting`

ヘッダー: `Authorization: Bearer <SOLAR_SECRET>`, `Content-Type: application/json`

**この経路にはリレー状態・リレー制御は含めない。** 片方向PUSHのみのフォールバック経路なので
コマンドを返しようがない（CLAUDE.mdの「EC2からデバイスへ接続しにいく設計にはしないこと」の原則を
この経路では変えない）。リレー制御は主経路（VPN）でのみ提供する。

**死活監視は `/report` の受信自体が兼ねる。** 専用のheartbeatイベントは無い。
送信間隔は**5分**（主経路がVPNで肩代わりするため、この経路は「主経路が落ちた時の命綱」的な
低頻度モニタリングでよい。当初案の60秒から変更 — 理由は§5.5・§6）。
ダッシュボードの「応答なし」判定・`SOLAR_TIMEOUT_SEC` の扱いも§5.5で見直す。

---

## 4. Modbusレジスタ対応表（フェーズ1用）

Rover公式のMODBUSプロトコル文書より。ファンクション0x03で読み取り、9600bps 8N1、スレーブアドレス1。

| ペイロードのキー | レジスタ | 換算 |
|---|---|---|
| `soc` | 0x0100 | そのまま |
| `batteryVoltage` | 0x0101 | ×0.1 |
| `chargingCurrent` | 0x0102 | ×0.01 |
| `controllerTemp` | 0x0103 上位バイト | b7が符号ビット |
| `batteryTemp` | 0x0103 下位バイト | b7が符号ビット |
| `loadPower` | 0x0106 | そのまま |
| `solarVoltage` | 0x0107 | ×0.1 |
| `solarCurrent` | 0x0108 | ×0.01 |
| `chargingPower` | 0x0109 | そのまま |
| `generationToday` | 0x0113 | そのまま(Wh) |
| `chargingState` | 0x0120 下位バイト | 0:停止 1:待機 2:MPPT 3:均等化 4:ブースト 5:フロート 6:電流制限 |
| `fault` | 0x0121〜0x0122 | U_DWORD。0なら正常 |

温度のデコード（0x0103は上位/下位に2つの温度が入り、各バイトのb7が符号）:
```cpp
uint8_t b = raw >> 8;   // controllerTemp（下位は raw & 0xFF）
return (b & 0x80) ? -(float)(b & 0x7F) : (float)b;
```

制御用（今回は使わないが記録）:

| レジスタ | 内容 |
|---|---|
| 0x010A | LOAD端子 ON/OFF（1/0）**日常操作に使わない。下の注意参照** |
| 0xE01D | 負荷動作モード。**17(0x11)=常時ON** に設定して運用する |

---

## 5. 開発フェーズ

ハードウェア到着済み（2026-08-21）。**安全確認 → ソフトウェアで検証できる部分を先に固める →
物理配線は最後**の順で進める。理由: §7の注意点（Rover LOAD端子から給電しているためLOAD OFFで
自分の電源を落としうる、RS485極性未確認）があり、後戻りしにくい物理配線を最後に固定したいため。
また副経路（直接POST）はVPN無しで完結するので、フェーズ2の時点で先行して本番投入し、
VPNハブ（フェーズ3〜5）が完成する前から実際の監視データをダッシュボードに出せる。

進行状況は各フェーズの `[ ]` を `[x]` に変えて記録する。

### フェーズ0: 安全確認（配線前）— 完了（2026-08-21）

- [x] RJ45のピン配置を実測（§7-3）。5=GND(0V) / 6=A(2.6V) / 7=B(2.7V) / 8=+5V(5V)。
      15V級のピンは無いためRS232(G1)ではない。ただし型番シールは`G2`表記無し（§5.6）。
      電圧パターン自体はRS485のアイドルバイアスとして妥当（RS232のマーク状態はマイナス側になるため
      2.6〜2.7Vという正の値とは整合しない＝RS485で矛盾しない、という消去法の確認に留まる）
- [ ] ESP32はUSB給電のみで動かす。バッテリーのLOAD端子への結線はフェーズ6まで行わない

### フェーズ1: ESP32ファームウェア

ESPHomeで2つの役割を持たせる。

1. **副経路（フォールバック監視）**: `http_request` コンポーネントで、§3の契約に沿って
   **5分間隔**でEC2へ直接POSTする
2. **主経路のコマンド受け口**: `web_server` コンポーネントを有効化するだけでよい
   （ESPHome標準機能で `switch` エンティティに対するREST APIが自動的に生える。
   独自のコマンド受信ロジックをファームウェア側に書く必要はない）。
   OrangePiのsolar-bridge（フェーズ3）がLAN内からこれを直接叩いてリレーを操作する

**電源についての補足（2026-08-21）**: 当初は「USB給電のまま通信確立→最後にLOAD端子へ」の順を
想定していたが、実機設置場所にUSB電源が無いため、**WiFi/起動の基本動作確認のみUSB給電（Mac接続）で
実施し、RS485接続・Modbus読み取りの確認はLOAD端子給電の状態で現場で行う**運用に変更。
これが許容できると判断した理由:
- 最も危険な「Load Terminal (DANGER: self power-off)」スイッチ（レジスタ0x010A）は
  ファームウェア側で `disabled_by_default: true` になっており、誤操作で自分の電源を
  落とす経路は塞がれている（`device/esp32/renogy-rover.yaml`）
- WiFi/起動自体は事前にUSB給電で検証済み

- [x] `esphome`（pipx経由）をMacにインストール
- [x] WiFi接続・基本起動をUSB給電で確認（2026-08-21、`TP-Link_49CC`に接続成功）
- [x] RS485(A/B/⏚)接続、LOAD端子給電で現場設置 → **Modbus読み取りは無応答のまま行き詰まり中。
      詳細な切り分け記録・次のアクションは§5.6を参照。BT-2モジュール到着待ちで中断（2026-08-23）**
- [ ] `http_request` コンポーネントで副経路POST（5分間隔、§3契約）を実装
- [ ] `web_server` コンポーネント有効化、Basic認証設定
- [ ] `SOLAR_SECRET`（副経路用）・`web_server`のBasic認証情報（主経路用、LAN内限定でも念のため設定）を
      ESPHomeの `secrets.yaml` に持たせる
- POSTの失敗はリトライせず握りつぶしてよい（次の5分で送り直されるため）
- **このYAMLの置き場所は要検討。** CatPoopWatchが別リポジトリなのに倣って切り出すか、
  API契約と密結合なのでHomeUtilities内に置き続けるか。現状は暫定でこのリポジトリに置いてある

---

## 5.6 Modbus無応答トラブルシューティング記録（2026-08-22〜23、BT-2到着待ちで中断中）

フェーズ1でRS485配線後、`esphome logs`で見るとModbusが**一度も応答を受信しない**（`Stop waiting for
response`が延々と続く）状態のまま行き詰まっている。**再開時はここから読めば経緯が分かるようにする。**

### 確定している事実

- Rover型番シール実測: **`RNG-CTRL-RVR20`のみ。`G2`の表記は無い**
- Rover本体は正常動作している（DC Home的な確認手段は無いが、本体としては生きている）
- ESP32側は正しいModbus RTUフレームを構築しUARTへ書き込めている
  （詳細ログで `01:03:01:11:00:01:D5:F3` のような妥当なフレームを確認済み）
- ESP32/WaveShare側のRS485送信は物理的に出ている（`0xAA`連続送信テストでA-B間の電圧の揺れをテスター
  で確認済み。テストコードは既に元に戻し済み）
- ケーブル導通は問題ない（真GND基準で 5=GND(0V) / 6=A(2.6V) / 7=B(2.7V) / 8=+5V(5V)、複数回再測定済み）

### 試して効果が無かったこと（すべて「完全無応答」のまま変化なし）

| 試したこと | 結果 |
|---|---|
| スレーブアドレス 0x01 / 0xFF / 0xF7 | 全て無応答 |
| A/B極性の入れ替え | 無応答 |
| GND(⏚)の接続あり/なし | 無応答 |
| ボーレート 9600 / 4800 / 19200 | 無応答 |
| pin8(+5V)に220Ω+LEDで負荷をかける（「アクセサリー電流検知」仮説の検証） | LEDは点灯（負荷は掛かった）が無応答 |

### 保留にした仮説

- **RS232説**: 汎用マニュアルPDF（別サイト経由）に「Communication: RS232 Modbus」とあり疑ったが、
  ユーザー手元の実物マニュアルには配線図が無く純正モジュール接続の説明のみで、電圧パターン
  （2.6〜2.7Vの正の値）もRS232のアイドル状態（マイナス側）とは整合しないため、
  **RS485で矛盾しないという結論に一旦戻している**（確証は無い）
- **G2限定の外部公開Modbus説**: 「G2」がRS485ポートの"生Modbus対応"を指す可能性を疑っていたが、
  BT-2モジュール自体もRS485ポート経由・Modbus形式でRoverと通信していることが分かった
  （下記参照）ため、Rover側が生Modbusに一切応答しないわけではなさそうという見方に傾いている。
  ただし非G2機で挙動が違う可能性はまだ排除できていない

### 解決（2026-08-27）: BT-2経由のBLEで疎通成功。Roverは正常と確定

**BT-2到着・購入・接続済み。** 進捗:

1. **BT-2 + スマホのDC Homeアプリで疎通確認 → 成功。** Roverは正常にRS485/Modbusに応答することが
   証明できた（Rover側は正常。原因は配線/アドレス側にあったと確定）
2. **Renogy公式のRJ45ピン配置を再調査した結果、重大な誤りが見つかった。**
   Web検索で「Renogy DC-DC unit RS-485 (RJ-45)」の情報を確認したところ、
   **ピン1=TX/RX+(A) / ピン2=TX/RX-(B) / ピン5=GND** が正しい配置の可能性が高い。
   これまで**ピン6/7をA/Bとして使っていたのが誤りだった**可能性がある
   （標準LANケーブルのT568B配線ではピン1-2は実際にツイストペアなので、
   信号品質の面でもこちらの方が理にかなっている）。
   **BT-2到着後、直結RS485の配線をピン1/2に変更して再検証していない（未実施）。次回ここから。**
3. **ESP32からBLE経由でBT-2に直接接続し、疎通に成功した。** 詳細は下記「BT-2 BLEプロトコル」参照。
   DC HomeアプリとBLE経由の両方でSOC=100%が一致することを確認済み（レジスタデコードは正しい）

### BT-2 BLEプロトコル（実証済み・2026-08-27）

MACアドレス: `7C:72:E7:2C:F5:47`（デバイス名 `BT-TH-E72CF547`。`esp32_ble_tracker`でスキャン確認済み）

GATTサービス/特性（[neilsheps/Renogy-BT2-Reader](https://github.com/neilsheps/Renogy-BT2-Reader)のリバースエンジニアリング結果通り）:
- 送信（コマンド書込）: サービス`0xFFD0` / 特性`0xFFD1`
- 受信（通知）: サービス`0xFFF0`（**FFD0ではない。別サービスなので注意**）/ 特性`0xFFF1`

コマンド形式（標準Modbus RTUとほぼ同じだが1点重要な違いがある）:
- **先頭バイトはRoverの実アドレス(0x01)ではなく、固定の`0xFF`**
- 以降は標準Modbus RTU（関数コード、レジスタ番地、レジスタ数、CRC16）
- CRCのバイト順は標準Modbusと同じLSBファースト（READMEの記載は分かりにくいが実際に計算して検証済み）
- 例: `FF 03 01 00 00 0B 10 2F`（0x0100番地から11レジスタ読み取り）
- 応答例: `FF.03.16.00.64...`（`16`=34=読み取ったバイト数、以降がレジスタ値、末尾2バイトがCRC）

実装場所: `device/esp32/renogy-rover.yaml`に一時テストコードとして`esp32_ble_tracker`/`ble_client`/
`sensor: platform: ble_client`(id: `bt2_notify_raw`)を追加済み。10秒毎に上記コマンドを送信し、
応答をログ出力するだけの検証用実装（本実装ではない）。

### 次に決めること: 直結RS485(ピン1/2) と BLE中継(BT-2常設)のどちらを本採用するか

| | 直結RS485（ピン1/2に変更して再テスト） | BLE中継（BT-2を挿しっぱなしにする） |
|---|---|---|
| 構成の単純さ | シンプル（追加ハードウェア無し） | BT-2という追加ハードウェアが常設で必要 |
| 実装コスト | 低い（既存のmodbus_controllerがそのまま使える） | 高い（BLE用に独自プロトコルを自前実装済みだが、本実装への統合が必要） |
| スマホアプリとの共存 | 問題なし | BT-2の同時接続数次第で排他になる可能性 |
| WiFi/BLE共存の安定性 | 影響なし | ESP32-S3は無線が共用のため、WiFi(§5.7で省電力設定済み)との干渉リスクあり |

**推奨: まずピン1/2への配線変更で直結RS485を再試行する。** これで直れば構成がシンプルになり、
BLE実装（せっかく検証はできたが本実装は未着手）は不要になる。直らなければBLE中継に切り替える。

### 元に戻し済みの一時変更

- YAMLの一時テスト用連続送信コード（`interval:`で0xAA送信）→ 削除済み
- スレーブアドレス → `0x01`（標準）に復帰済み
- ボーレート → `9600`（標準）に復帰済み
- `logger:`は`VERY_VERBOSE`のまま（`uart`/`modbus`/`modbus_controller`）。再開時に必要なければ
  `INFO`に戻してよい
- `api: reboot_timeout: 0s` を追加済み（無関係の別バグ。クライアント無しで15分毎に自動再起動する
  デフォルト挙動を無効化した。これは今後も維持してよい）
- `wifi: power_save_mode: light` を追加済み（§5.7参照）

---

## 5.7 電力収支とリレー制御機能（駐車場LED、着手前の検討メモ・2026-08-23）

### 電力収支の計算結果

- バッテリー: LiTime 12.8V 20Ah（LiFePO4） = 約256Wh
- パネル: 100W。実効発電量は天候により約240Wh/日（曇り・冬）〜約480Wh/日（快晴）
- 負荷: 駐車場LED 0.6W×10個=6W（夜間点灯、季節で9〜14時間）＋ESP32基板（常時約1.3〜2.5W）
  → 合計消費は約90〜145Wh/日
- **結論: 曇りの日でも約95〜215Wh/日の余剰がある。** 現状のパネル・バッテリー構成に無理は無い

### バッテリー拡張（検討のみ、未着手）

- LiTime LiFePO4は同型4台までの並列接続が一般的な上限（同容量・同時期購入、個別満充電してから
  接続、バスバー配線、個別ヒューズが条件）
- 100Wパネル固定なら、拡張の効果は「日々の収支改善」ではなく**「悪天候連続時の自立日数を延ばす」**
  こと。現状20Ah(256Wh)で自立約1.8〜2.8日、80Ah(1024Wh・4台)なら約7〜11日
- Roverの充電電流(20A)はバッテリー総量の上限にはならない（大きいバンクほど満充電に時間がかかる
  だけ）。実質的なボトルネックはパネルの発電量
- 現状は電力収支に余裕があるため急ぎではないが、梅雨・台風等への備えとして検討の価値はある

### ESP32側の省電力設定

- `wifi: power_save_mode: light` を追加（開発・運用共通の恒久設定として採用）
- **ディープスリープは不採用と判断した。** 理由: §5.5のリレー即時制御・常時Modbus監視・
  今後実装するスケジュール消灯機能のいずれとも矛盾する
  （スリープ中は命令を受け取れず、リレーのGPIO出力状態も維持されない可能性がある）。
  電力収支にも余裕があるためディープスリープの必要性自体が薄い

### 駐車場LED リレー制御機能（次のタスク、詳細未確定）

**背景**: 現在、駐車場LED照明はこのソーラーシステムで動作しており、既存の**別の（ESP32とは別系統の）
リレー**が照度センサーで暗くなると自動点灯している。ただし消灯のタイミングが無く、朝まで点灯し
続けてしまう。

**要望（優先順）**:
1. 既存の「暗くなったら点灯」機能はそのまま維持しつつ、**時刻指定で消灯する機能を追加**
2. まずは**点灯状態をHomeUtilityダッシュボードから制御できるようにする**（手動ON/OFF）
3. 将来的な発展: カメラ・人感センサーと連動した点灯制御（未着手・検討段階）

**未確定で次回詰める点**:
- 物理配線: ESP32のリレー(CH1=`dc_output`を流用想定)を、既存の照度センサー式リレー回路に**直列に
  割り込ませ、ESP32側リレーがOFFの時は強制消灯できるようにする**という理解で合っているか要確認
- 制御範囲: 自宅LAN内（ESP32のWebUI直）でまず動かすか、EC2ダッシュボード（VPNハブ経由、§5.5）
  まで最初から必要か
- Modbus無応答問題（§5.6）とは独立して進められる（GPIO直結のリレーなのでRover通信に依存しない）

---

### フェーズ2: 副経路を先行して本番投入

```bash
ssh ec2-user@54.178.201.84 "openssl rand -hex 32"   # SOLAR_SECRET を生成
# ~/homeutils/.env に SOLAR_SECRET=<値> を追記
scp -r ./server/* ec2-user@54.178.201.84:~/homeutils/
pm2 restart homeutils-gateway
curl https://www.cetacea.jp/home/api/solar/status    # {} 相当が返ればOK
```

- [ ] EC2に `SOLAR_SECRET` 設定・デプロイ
- [ ] ESP32→EC2の5分間隔POSTが実際に通り、ダッシュボードの🔋カードが更新されることを確認
- **この時点で「監視だけ」は本番運用に入れる**（VPN未完成でも成立）

### フェーズ3: OrangePi — solar-bridge（新規実装）

CatPoopWatchと同じOrangePiに、独立したsystemdサービスとして相乗りさせる
（HOMESERVER.md §5で「要検討」だった論点はこれで決着 — 相乗り採用）。

役割:
- ESP32のModbus状態をLAN内で定期取得（ESP32の `web_server` APIから読むか、ESP32側から
  OrangePiのローカルIPへ短間隔POSTさせるかは実装時に決める。後者の方がESP32側の実装が
  副経路と対称になり単純）
- 最新状態をメモリ/ローカルファイルにキャッシュ
- Tailscale経由でEC2からの以下のリクエストを受ける:
  - `GET /status` — キャッシュ済み最新状態を返す
  - `POST /relay` — ESP32の `web_server` APIへ即座に中継し、結果を返す
- **Tailscaleインターフェースにのみbind**し、自宅LAN全体を晒さない
  （0.0.0.0でリッスンしない。ここを誤ると自宅LAN内の他機器がTailscale経由で見える事故になる）

- [ ] solar-bridgeサービスの実装
- [ ] systemdユニット作成、CatPoopWatchと競合しないポート割り当て
- [ ] **VPN無しで**LAN内のみで動作確認（OrangePiからESP32のリレーを操作できることを確認）

### フェーズ4: Tailscale VPN構築

- [ ] EC2・OrangePi双方にTailscaleを導入し、同一tailnetに参加させる
- [ ] Tailscale ACLで、EC2からOrangePiへは「solar-bridgeのポートのみ」到達可能に絞る
      （Tailscale個人プランの範囲で構成可能）
- [ ] OrangePiが再起動・ネットワーク断からどう自動復帰するか確認（`tailscale up` の永続化、
      systemd依存関係）
- [ ] EC2からOrangePiのTailscale IPへ直接 `curl` できることを確認
- **CLAUDE.mdの「EC2からデバイスへ接続しにいく設計にはしないこと」を更新済み**（§5.5参照）

### フェーズ5: EC2側 — 主経路の実装

- [ ] `server/features/solar/routes.js` にOrangePi中継ロジック追加
      （`GET status` / `POST relay` のプロキシ）
- [ ] ダッシュボードUI: リレー操作ボタンを追加
- [ ] ダッシュボードUI: 「リアルタイム更新中／フォールバックのみ／応答なし」の表示切り替え
- [ ] `SOLAR_TIMEOUT_SEC` を副経路の間隔（5分）基準に見直す（§5.5参照）

### フェーズ6: 最終物理配線

- [ ] RoverのLOAD端子・実際の外部機器へのリレー配線を確定
      （§7-5: 通信確立後に電源をLOAD端子へ移す順序を厳守）

### フェーズ7: 通し運用確認

- [ ] 主経路・副経路を同時稼働させ、数日運用
- [ ] OrangePi再起動時の自動復帰を確認
- [ ] VPN切断時にフォールバックモードへ正しく切り替わることを確認

---

## 5.8 UI: ダッシュボード組み込みから専用画面へ（2026-08-27）

**変更内容**: 太陽光カードを`/home/control/`（Home Controlダッシュボード）から切り出し、
Bus・Catと同格の**専用タブ `/home/solar/`** にした。CLAUDE.mdの「マウントの2パターン」の
記載も、solarを「ダッシュボード組み込み型」から「専用画面を持つ機能」側に更新済み。

**理由**: 当初はcatwatchに倣ってダッシュボード組み込み型にしていたが、ユーザーから
「バス・猫に続く3つ目のタブとして太陽光を独立させたい」との要望があったため。

**変更したファイル**:
- `server/public/solar/index.html` — **新規**。`home/index.html`から🔋カードのHTML/CSS/JSを
  そのまま移植し、Bus/Cat/Solarのナビを追加
- `server/public/home/index.html` — 🔋カード関連のHTML/CSS/JSを削除。ナビにSolarタブを追加
- `server/public/bus/index.html` — ナビにSolarタブを追加
- `server/public/app/index.html`（iOSホーム画面アプリシェル） — 3つ目のiframeタブ`solar`を追加
- `server/server.js` — `app.use('/home/solar', express.static(...))`を追加。
  `server/features/solar/routes.js`（`/home/api/solar/*`）は変更無し

**確認方法**: ローカルでは`node server.js`がpuppeteer/yargs起因の既存バグ
（Node v26.5.0とCJS/ESM相互運用の非互換、本タスクとは無関係の環境問題）で起動できなかったため、
`python3 -m http.server`で`server/public/`を直接配信し、4画面（bus/home/solar/app）が
200で返り、`home/index.html`に太陽光関連コードが残っていないこと、`app/index.html`の
`frame-solar`参照が正しいことを確認した。**Express経由での実地確認はまだ**（環境問題を
別途解消してから行うこと）。

---

## 5.5 VPNハブ設計（背景と設計根拠）

### 経緯

当初案（ESP32から60秒間隔でEC2へ直接POST）には2つの課題があった:

1. `gateway.js` のワーカーは1時間無通信で終了する設計だが、60秒間隔の定期POSTを受け続けると
   永久に終了しなくなり、アイドル終了によるリソース節約の狙いが効かなくなる
2. iPhoneからのリレー操作（外部機器の電源ON/OFF）を実現するには、EC2からESP32へ
   コマンドを届ける経路が要る。しかし「自宅デバイスはNAT内・EC2から到達不可・
   常にデバイス側が起点」という原則（CLAUDE.md）の下では、EC2発のコマンドは
   デバイス側のポーリング応答に乗せるしかなく、ポーリング間隔＝操作の反映レイテンシになる

この2つを両立よく解決する方法として、**OrangePi（CatPoopWatchと同居）にTailscaleを常時接続させ、
EC2からOrangePi経由でESP32へオンデマンドに到達できる経路を主経路とする**方式を採用した。

### 採用理由

- **リレー操作が即時になる。** ポーリング間隔を待たず、iPhoneでボタンを押した瞬間に
  `EC2 → Tailscale → OrangePi → LAN → ESP32` で数百ms〜数秒で反映できる
- **ワーカーのアイドル終了問題が構造的に解消する。** Tailscaleのトンネル維持自体は
  OSレベルのVPN接続で、`server.js` の `lastRequestTime`（HTTPリクエストで更新される）とは
  無関係。定期監視をOrangePi↔ESP32のLAN内だけで完結させ、EC2側は**ダッシュボードを
  開いている人がいる時だけ**OrangePi経由で最新値を取りに行く設計にできるため、
  「60秒ごとに必ずEC2へPOSTする」トラフィック自体をやめられる
- **VPN常時接続の負荷は無視できる。** WireGuard/Tailscaleのkeepaliveは数十バイト・
  数十秒間隔で、CPU・メモリともにEC2側（既存のApache+Node worker）・OrangePi側
  （既存のCatPoopWatch常駐プロセス）どちらにも実質的な追加負荷にならない。
  EC2は既にcetacea.jpの固定パブリックIPを持っているため、Elastic IP等の追加調達も不要

### 引き受けたコストとその手当て

| リスク | 手当て |
|---|---|
| OrangePi/VPNが落ちると監視・制御が両方止まる（単一障害点が増える） | **副経路**（ESP32→EC2直接POST、5分間隔）を並存させ、監視だけは生き残らせる。リレー制御は主経路のみ（不可の場合はダッシュボード側でボタンを無効化） |
| VPNトンネルにより、原理上EC2から自宅LANへの到達経路が生まれる | solar-bridgeはTailscaleインターフェースのみにbind。Tailscale ACLでEC2→OrangePiの到達範囲をsolar-bridgeのポートのみに制限 |
| CLAUDE.mdの既存原則と矛盾する | CLAUDE.mdを更新し、「VPNトンネルは自宅側デバイスが自発的に確立する場合に限り例外」と明記（ホームルーターのポート開放や、EC2発の非トンネル接続は引き続き禁止） |

### 二経路の使い分けとダッシュボード表示

- **主経路（VPN）が生きている間**: ダッシュボードは「リアルタイム更新中」を表示し、リレー操作ボタンが有効
- **主経路が落ち、副経路（5分間隔フォールバック）のみの間**: 「フォールバックモード（5分間隔・制御不可）」を表示し、リレー操作ボタンは無効化。監視データ自体は5分遅れで更新され続ける
- **どちらも途絶えた場合**: 従来通り「応答なし」。ただししきい値`SOLAR_TIMEOUT_SEC`は
  副経路の間隔（5分）を基準に緩める必要がある（例: 400秒。60秒間隔前提だった現行の180秒のままだと、
  正常にフォールバック動作している間も誤って「応答なし」と表示してしまう）。
  `server/public/home/index.html` の該当箇所は実装時に見直す

---

## 6. 設計判断と理由

- **状態はメモリ保持**（catwatchと同じ）。ワーカーが終了しても、ESP32が次のレポートを送れば
  5分で復元されるため永続化していない。OrangePi側solar-bridgeは常時稼働なので、
  こちらは状態をローカルファイルにキャッシュする（フェーズ3）。
- **送られなかったキーは前回値を維持する。** 全キー必須にすると、ESP32側で一部のセンサーが
  読めなかっただけでダッシュボード全体が欠測表示になるため。
- **範囲チェックを入れてある。** Modbus通信エラー時の異常値や NaN がそのままSSEに流れると
  ダッシュボードの表示が壊れるため、範囲外は「値なし」として前回値を残す。
- **専用heartbeatを設けていない。** catwatchはイベントが疎（糞検知は稀）なので死活監視が
  別途必要だったが、solarは定期レポート自体が密なのでそれで足りる（副経路の5分間隔でも同様）。
- **`soc: 0` と「値なし」を区別している。** `if (value)` ではなく `!== null` で判定。
- **監視は二経路、制御は一経路のみ。** リレー制御（外部機器の電源ON/OFF）はVPN経由の
  主経路でしか提供しない。副経路（ESP32→EC2直接POST）は片方向PUSHのみを保ち、
  CLAUDE.mdの「EC2からデバイスへ接続しにいく設計にはしないこと」の原則をこの経路では
  一切変えていない。原則の例外はOrangePiが自発的に張るVPNトンネルに限定している（§5.5）。
- **OrangePiは既存のCatPoopWatch常駐機に相乗りさせる方針とした。** 別デバイスを新規に
  用意するより運用の手間が少なく、Tailscaleの追加負荷も無視できる水準のため
  （HOMESERVER.md §5で「要検討」だった論点の決着）。

---

## 7. 運用上の注意（実機接続後）

1. **`0x010A`（LOAD端子ON/OFF）をModbusで日常操作しないこと。**
   ESP32自身がLOAD端子から給電されているため、OFFにすると自分の電源が落ちて
   遠隔で復帰できなくなる。DC負荷のON/OFFは基板の内蔵リレーCH1（GPIO1）で行う。
2. **RS485はA/B/GND(基準線)の3本を結線する（2026-08-21改訂）。**
   Rover側RJ45のpin5は、実測の結果バッテリー/LOAD側から絶縁された、RS485ポート専用の
   浮いた基準電位であることが分かった（LOAD マイナス基準では検知できず、pin5基準でのみ
   A/Bの電圧差を検知できた）。Waveshare機のRS485端子台もA/B/アースマークの3ピン構成で、
   3つ目は「RS485」の囲みの中にあり保護アースではなくRS485信号用の基準線と判断。
   pin5をWaveshare機のこの3つ目の端子に接続する。
   （当初「GNDは繋がない」としていたが、これは絶縁の意味を電源系グランドループ防止と
   誤って一般化していたための誤り。信号系の基準線とESP32本体の電源グランドは別物）
3. **RJ45のピン配置は実測確認済み（2026-08-21）。** Renogyは公式にピン配置を公開していないため、
   実機で確認した。真GND基準で ピン5=GND(0V) / ピン6=A(2.6V) / ピン7=B(2.7V) / ピン8=+5V(5V)。
   想定通りでG1(RS232)の心配は無し（12〜16V級のピンは無かった）。
4. **低電圧遮断(LVD)が働くとESP32も停止し、監視データが途切れる。**
   バッテリー保護としては正常な挙動。
5. **立ち上げは必ずUSB給電のまま通信を確立させ、最後に電源をLOAD端子へ移すこと。**
   順序を逆にすると、設定ミスでLOAD端子がOFFになった際に手が出せなくなる。

---

## 8. 参考資料

- Rover MODBUSプロトコル公式PDF: [ESP32ArduinoRenogy](https://github.com/wrybread/ESP32ArduinoRenogy) に同梱
- [Waveshare ESP32-S3-Relay-6CH 公式](https://www.waveshare.com/esp32-s3-relay-6ch.htm)
- [ESPHome Devices — 同機のピン配置](https://devices.esphome.io/devices/waveshare-6ch-relay/)
  （RS485: TX=GPIO17 / RX=GPIO18・方向自動制御、リレーCH1=GPIO1）
- 実装の参考: `server/features/catwatch/routes.js`（Webhook受信・SSE・認証の前例）
