# 本番用テンプレート（.env にコピーして値を設定）
AUTH_USER=YOUR_USERNAME
AUTH_PASS=YOUR_PASSWORD
BEHIND_APACHE=true
CATWATCH_SECRET=YOUR_SECRET_TOKEN
# 太陽光発電コントローラー（ESP32）→ EC2 の Webhook 認証用。
# openssl rand -hex 32 で生成し、ESP32 側の設定と同じ値にする。
SOLAR_SECRET=YOUR_SOLAR_SECRET_TOKEN
